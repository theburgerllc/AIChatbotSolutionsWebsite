# Prisma CLI environment (local example)
# This file is committed. Do not put secrets here.
# Create prisma/.env with real values (ignored by git).

# SQLite (default local dev)
DATABASE_URL="file:./dev.db"

# If you use a shadow database for migrations (optional)
# SHADOW_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/app_shadow?schema=public"

